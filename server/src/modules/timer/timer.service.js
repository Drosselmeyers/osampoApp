function createTimerService(repository) {
  const createPostUserTimer = async () => {};
  const patchTimerStatus = async () => {};
  const resetUserTimer = async () => {};

  return { createPostUserTimer, patchTimerStatus, resetUserTimer };
}

module.exports = { createTimerService };
